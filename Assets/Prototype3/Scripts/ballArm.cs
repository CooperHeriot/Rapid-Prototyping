using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ballArm : MonoBehaviour
{
    public HingeJoint HJ1, HJ2;

    public float Spring1, spring2;
    // Start is called before the first frame update
    void Start()
    {
       // HJ1 = GetComponent<HingeJoint>();
       // HJ2 = GetComponent<HingeJoint>();
    }

    // Update is called once per frame
    void Update()
    {
        //Spring1 = HJ1.spring.targetPosition;

        //HJ1.spring.targetPosition = Spring1;

        
    }
}
